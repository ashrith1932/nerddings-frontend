# Copy these values into the frontend hosting provider's production environment.
# Only NEXT_PUBLIC_* values are exposed to the browser.
NEXT_PUBLIC_APP_URL=https://thepeoplesrepellentparty.in
NEXT_PUBLIC_API_URL=https://api.thepeoplesrepellentparty.in/api/v1
