"use client";

import { useEffect, useMemo, useState } from "react";
import { Activity, ArrowUpRight, Bell, Bookmark, BriefcaseBusiness, CalendarDays, Check, Flame, Loader2, MessageCircle, Search, Sparkles, TrendingUp, Users } from "lucide-react";
import { apiFetch, getAuthToken } from "@/lib/api";
import MessagesPanel from "@/components/messages/MessagesPanel";
import HomeFeedSurface from "@/components/social/HomeFeedSurface";

const panelCss = `
.live-data-route{width:100%;padding:4px 0 60px}.live-data-route h1{margin:4px 0 7px;font-size:28px}.live-data-route p{color:#8b8178;font-size:12px;line-height:1.55}.live-data-head{display:flex;justify-content:space-between;align-items:flex-end;gap:18px;margin-bottom:18px}.live-data-head .eyebrow,.live-data-kicker{font-size:8px;letter-spacing:.14em;font-weight:800;color:#978d84}.live-data-tabs{display:flex;gap:20px;border-bottom:1px solid var(--line,#ddd6cc);margin-bottom:14px}.live-data-tabs button{border:0;background:none;padding:11px 0;color:#8b8178;font-weight:700;font-size:12px;cursor:pointer;position:relative}.live-data-tabs button.active{color:var(--ink,#201c19)}.live-data-tabs button.active:after{content:"";position:absolute;left:0;right:0;bottom:-1px;height:2px;background:var(--ink,#201c19)}.live-data-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.live-data-card{background:var(--card,#fff);border:1px solid var(--line,#ddd6cc);border-radius:12px;padding:15px;box-shadow:0 2px 8px rgba(31,27,24,.03)}.live-data-card:hover{border-color:#c7bdb2;box-shadow:0 10px 26px rgba(31,27,24,.06)}.live-data-card-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.live-data-card h3{font-size:13px;margin:4px 0}.live-data-meta{font-size:9px;color:#978d84}.live-data-row{display:flex;align-items:center;gap:10px;padding:11px 0;border-bottom:1px solid #eee8e1}.live-data-row:last-child{border-bottom:0}.live-data-rank{width:25px;font-size:10px;color:#988e85;font-weight:800}.live-data-row-main{flex:1;min-width:0}.live-data-row-main strong{font-size:11px;display:block}.live-data-row-main span{font-size:9px;color:#8e847b}.live-data-score{font-size:11px;font-weight:800}.live-data-progress{height:5px;border-radius:99px;background:#eee9e3;overflow:hidden;margin-top:6px}.live-data-progress i{display:block;height:100%;background:var(--accent,#d35d34);border-radius:99px}.live-data-notification{display:flex;gap:10px;align-items:flex-start;padding:13px 0;border-bottom:1px solid #eee8e1;cursor:pointer}.live-data-notification.unread{background:#fbf7f1;margin:0 -8px;padding-left:8px;padding-right:8px;border-radius:8px}.live-data-notification-icon{width:32px;height:32px;border-radius:9px;background:#f0ebe4;display:grid;place-items:center;color:#6e655d;flex:0 0 auto}.live-data-notification-main{flex:1;min-width:0}.live-data-notification-main strong{font-size:11px}.live-data-notification-main p{margin:3px 0;font-size:10px}.live-data-notification-main small{font-size:8px;color:#9a9087}.live-data-unread{width:6px;height:6px;border-radius:50%;background:var(--accent,#d35d34);margin-top:8px}.live-data-empty{min-height:220px;display:grid;place-items:center;text-align:center;border:1px solid var(--line,#ddd6cc);border-radius:12px;background:var(--card,#fff);padding:30px}.live-data-empty svg{color:#9a9087}.live-data-empty strong{font-size:12px}.live-data-empty span{font-size:10px;color:#948a81}.live-data-fundraising{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.live-fund-card{background:var(--card,#fff);border:1px solid var(--line,#ddd6cc);border-radius:12px;padding:15px}.live-fund-top{display:flex;justify-content:space-between;align-items:flex-start}.live-fund-top strong{font-size:14px}.live-fund-stage{font-size:8px;padding:4px 7px;border:1px solid var(--line);border-radius:99px}.live-fund-amount{font-size:20px;font-weight:800;margin:13px 0 2px}.live-fund-sub{font-size:9px;color:#948a81}.live-fund-progress{height:7px;background:#eee9e3;border-radius:99px;overflow:hidden;margin:12px 0 7px}.live-fund-progress i{display:block;height:100%;background:var(--accent,#d35d34)}.live-fund-foot{display:flex;justify-content:space-between;color:#8e847b;font-size:9px}.live-search-box{display:flex;align-items:center;gap:8px;border:1px solid var(--line);border-radius:10px;background:var(--card);padding:9px 11px;margin-bottom:14px}.live-search-box input{border:0;outline:0;flex:1;background:transparent;font:inherit;font-size:12px}.live-search-section{margin-bottom:18px}.live-search-section h2{font-size:13px;margin:0 0 8px}.live-search-item{display:flex;align-items:center;gap:9px;border-bottom:1px solid #eee8e1;padding:10px 0}.live-search-item strong{font-size:11px}.live-search-item small{display:block;color:#91877e;font-size:9px;margin-top:2px}.live-search-link{border:0;background:none;padding:0;text-align:left;cursor:pointer;color:inherit;flex:1}.live-messages-wrap{min-height:500px}.live-messages-wrap>div{min-height:500px}
@media(max-width:800px){.live-data-grid,.live-data-fundraising{grid-template-columns:1fr}.live-data-head{align-items:flex-start;flex-direction:column}.live-data-route h1{font-size:24px}}
`;

const ago = (value: string) => { const seconds = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 1000)); if (seconds < 60) return "now"; if (seconds < 3600) return `${Math.floor(seconds / 60)}m`; if (seconds < 86400) return `${Math.floor(seconds / 3600)}h`; return `${Math.floor(seconds / 86400)}d`; };
const initials = (name: string) => name.split(/\s+/).filter(Boolean).map((part) => part[0]).join("").slice(0,2).toUpperCase();

function LiveNavMeta() {
  useEffect(() => {
    const refresh = async () => {
      const status = document.querySelector<HTMLElement>(".status-chip");
      if (status) status.style.display = "none";
      if (!getAuthToken()) return;
      try {
        const [notifications, messages] = await Promise.allSettled([apiFetch<any>("/notifications"), apiFetch<any>("/social/messages/unread-count")]);
        const counts = { Messages: messages.status === "fulfilled" ? Number(messages.value?.data?.unreadCount ?? 0) + Number(messages.value?.data?.pendingRequests ?? 0) : 0, Notifications: notifications.status === "fulfilled" ? Number(notifications.value?.unreadCount ?? 0) : 0 };
        document.querySelectorAll<HTMLElement>(".nav-item").forEach((item) => { const label = item.querySelector("span")?.textContent?.trim() as keyof typeof counts | undefined; if (!label || !(label in counts)) return; const count = counts[label]; let badge = item.querySelector<HTMLElement>("b"); if (count <= 0) { badge?.remove(); return; } if (!badge) { badge = document.createElement("b"); item.appendChild(badge); } badge.textContent = count > 99 ? "99+" : String(count); badge.style.display = "inline-flex"; });
      } catch {}
    };
    void refresh();
    const timer = window.setInterval(() => void refresh(), 15000);
    return () => window.clearInterval(timer);
  }, []);
  return null;
}

export function LiveHomeRoute() { return <HomeFeedSurface />; }

export function LiveMessagesRoute() { return <div className="live-data-route live-messages-wrap"><style>{panelCss}</style><LiveNavMeta /><MessagesPanel /></div>; }

export function LiveNotificationsRoute() {
  const [items, setItems] = useState<any[]>([]); const [loading, setLoading] = useState(true);
  const load = async () => { setLoading(true); try { const response = await apiFetch<any>("/notifications"); setItems(response.data ?? []); } catch { setItems([]); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const markAll = async () => { try { await apiFetch("/notifications/read-all", { method: "POST" }); await load(); } catch {} };
  return <div className="live-data-route"><style>{panelCss}</style><LiveNavMeta /><div className="live-data-head"><div><div className="eyebrow">KEEP IN THE LOOP</div><h1>Notifications</h1><p>Live activity generated by your Nerdding network.</p></div><button className="outline-button" onClick={() => void markAll()}><Check size={14} /> Mark all as read</button></div>{loading ? <div className="live-data-empty"><Loader2 size={20} /></div> : items.length ? <div className="live-data-card">{items.map((item) => <div className={`live-data-notification ${!item.readAt ? "unread" : ""}`} key={item.id} onClick={async () => { if (!item.readAt) { try { await apiFetch(`/notifications/${item.id}/read`, { method: "POST" }); } catch {} } if (item.kind === "follow" && item.actor?.username) window.history.pushState({}, "", `/profile/${encodeURIComponent(item.actor.username)}`); else if (item.entityId) window.history.pushState({}, "", `/post/${encodeURIComponent(item.entityId)}`); window.dispatchEvent(new PopStateEvent("popstate")); }}><span className="live-data-notification-icon">{item.kind === "follow" ? <Users size={15} /> : item.kind === "comment" ? <MessageCircle size={15} /> : item.kind === "message_request" ? <Bell size={15} /> : <Activity size={15} />}</span><span className="live-data-notification-main"><strong>{item.actor?.name ?? "Nerdding"}</strong><p>{item.text}</p><small>{ago(item.createdAt)}</small></span>{!item.readAt && <i className="live-data-unread" />}</div>)}</div> : <div className="live-data-empty"><Bell size={20} /><strong>No notifications</strong><span>New activity will appear here when something happens.</span></div>}</div>;
}

export function LiveChartsRoute() {
  const [data, setData] = useState<any>(null); const [tab, setTab] = useState("risingBuilders"); const [loading, setLoading] = useState(true);
  useEffect(() => { apiFetch<any>("/charts").then((response) => setData(response.data)).catch(() => setData(null)).finally(() => setLoading(false)); }, []);
  const rows = useMemo(() => data?.[tab] ?? [], [data, tab]);
  return <div className="live-data-route"><style>{panelCss}</style><LiveNavMeta /><div className="live-data-head"><div><div className="eyebrow">SIGNALS OVER STATUS</div><h1>Top charts.</h1><p>Live ranking data from proof, trust and meaningful activity.</p></div></div><div className="live-data-tabs"><button className={tab === "risingBuilders" ? "active" : ""} onClick={() => setTab("risingBuilders")}>Rising builders</button><button className={tab === "topProjects" ? "active" : ""} onClick={() => setTab("topProjects")}>Top projects</button><button className={tab === "trendingStartups" ? "active" : ""} onClick={() => setTab("trendingStartups")}>Trending startups</button></div>{loading ? <div className="live-data-empty"><Loader2 size={20} /></div> : rows.length ? <div className="live-data-card">{rows.map((row: any, index: number) => <div className="live-data-row" key={row.id ?? index}><span className="live-data-rank">{String(index + 1).padStart(2, "0")}</span><span className="home-avatar home-avatar-sm">{row.avatarUrl ? <img src={row.avatarUrl} alt="" /> : initials(row.name ?? row.startupName ?? row.title ?? "N")}</span><span className="live-data-row-main"><strong>{row.name ?? row.startupName ?? row.title}</strong><span>{row.username ? `@${row.username}` : row.stage ?? row.industry ?? "Live signal"}</span><span className="live-data-progress"><i style={{ width: `${Math.min(100, Math.max(8, Number(row.score ?? row.progress ?? 0) * (row.score ? 100 : 1)))}%` }} /></span></span><span className="live-data-score">{row.score != null ? Number(row.score).toFixed(2) : row.progress != null ? `${row.progress}%` : "—"}</span></div>)}</div> : <div className="live-data-empty"><TrendingUp size={20} /><strong>No live rankings yet</strong><span>The backend has no records for this chart.</span></div>}</div>;
}

export function LiveFundraisingRoute() {
  const [items, setItems] = useState<any[]>([]); const [loading, setLoading] = useState(true);
  useEffect(() => { apiFetch<any>("/fundraisings").then((response) => setItems(response.data ?? [])).catch(() => setItems([])).finally(() => setLoading(false)); }, []);
  return <div className="live-data-route"><style>{panelCss}</style><LiveNavMeta /><div className="live-data-head"><div><div className="eyebrow">FUNDRAISING</div><h1>Capital for builders.</h1><p>Active fundraising profiles published by verified agents.</p></div></div>{loading ? <div className="live-data-empty"><Loader2 size={20} /></div> : items.length ? <div className="live-data-fundraising">{items.map((item) => <article className="live-fund-card" key={item.id}><div className="live-fund-top"><strong>{item.startupName}</strong><span className="live-fund-stage">{item.stage}</span></div><div className="live-fund-amount">{item.currency} {Number(item.raisedAmount).toLocaleString()}</div><div className="live-fund-sub">raised of {Number(item.targetAmount).toLocaleString()}</div><div className="live-fund-progress"><i style={{ width: `${item.progress}%` }} /></div><div className="live-fund-foot"><span>{item.industry}</span><span>{item.investorCount} investors</span></div></article>)}</div> : <div className="live-data-empty"><BriefcaseBusiness size={20} /><strong>No fundraising profiles yet</strong><span>Verified agents can publish fundraising opportunities here.</span></div>}</div>;
}

export function LiveSearchRoute({ query }: { query: string }) {
  const [data, setData] = useState<any>(null); const [loading, setLoading] = useState(false);
  useEffect(() => { const q = query.trim(); if (!q) { setData(null); return; } setLoading(true); const timer = window.setTimeout(() => { apiFetch<any>(`/search?q=${encodeURIComponent(q)}`).then((response) => setData(response.data)).catch(() => setData(null)).finally(() => setLoading(false)); }, 250); return () => window.clearTimeout(timer); }, [query]);
  const section = (title: string, items: any[], href: (item: any) => string) => <section className="live-search-section"><h2>{title}</h2>{items.length ? items.map((item) => <div className="live-search-item" key={item.id}><span className="home-avatar home-avatar-sm">{item.avatarUrl ? <img src={item.avatarUrl} alt="" /> : initials(item.name ?? item.title ?? "N")}</span><button className="live-search-link" onClick={() => { window.history.pushState({}, "", href(item)); window.dispatchEvent(new PopStateEvent("popstate")); }}><strong>{item.name ?? item.title}</strong><small>{item.username ? `@${item.username}` : item.description ?? item.body}</small></button><ArrowUpRight size={14} /></div>) : <p>No results.</p>}</section>;
  return <div className="live-data-route"><style>{panelCss}</style><LiveNavMeta /><div className="live-data-head"><div><div className="eyebrow">SEARCH</div><h1>Search Nerdding.</h1><p>Results come directly from people, projects and posts in the production database.</p></div></div><div className="live-search-box"><Search size={16} /><input value={query} readOnly placeholder="Search people, projects, ideas" /></div>{!query.trim() ? <div className="live-data-empty"><Search size={20} /><strong>Start searching</strong><span>Enter a name, project or idea in the top search box.</span></div> : loading ? <div className="live-data-empty"><Loader2 size={20} /></div> : data ? <div className="live-data-card">{section("People", data.users ?? [], (item) => `/profile/${encodeURIComponent(item.username)}`)}{section("Projects", data.projects ?? [], (item) => `/project/${encodeURIComponent(item.slug)}`)}{section("Posts", data.posts ?? [], (item) => `/post/${encodeURIComponent(item.id)}`)}</div> : <div className="live-data-empty"><Search size={20} /><strong>No results</strong><span>Try a different search term.</span></div>}</div>;
}
