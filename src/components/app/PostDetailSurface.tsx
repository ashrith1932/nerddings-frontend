"use client";

import { useEffect, useState } from "react";
import { ArrowLeft, Heart, Send, Bookmark, Activity, Eye, MessageCircle, Link2 } from "lucide-react";
import { apiFetch, getSavedUser } from "@/lib/api";
import { Avatar } from "@/components/ui/Avatar";
import ThreadedCommentTree, { type ThreadComment } from "@/components/social/ThreadedCommentTree";

type UserRef = { id: string; name: string; username: string; avatarUrl?: string | null };
type Media = { publicUrl: string | null; mimeType: string };
type QuotePost = { id: string; text: string; createdAt: string; author: UserRef; media?: Media[] };
type Post = { id: string; text: string; createdAt: string; likes: number; comments: number; reposts: number; saves?: number; views?: number; author: UserRef; project?: { name: string; slug: string } | null; linkUrl?: string | null; media?: Media[]; quotePostId?: string | null; quotePost?: QuotePost | null; commentsTree: ThreadComment[] };
const nav=(p:string)=>{window.history.pushState({},"",p);window.dispatchEvent(new PopStateEvent("popstate"))};
const ago=(value:string)=>{const s=Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/1000));if(s<60)return"now";if(s<3600)return`${Math.floor(s/60)}m`;if(s<86400)return`${Math.floor(s/3600)}h`;return`${Math.floor(s/86400)}d`};

function UserButton({user,size="xs"}:{user:UserRef;size?:"xs"|"sm"|"md"}){return <button className="post-detail-author-button" onClick={()=>nav(`/profile/${encodeURIComponent(user.username)}`)}><Avatar user={user} size={size}/><span><strong>{user.name}</strong><small>@{user.username}</small></span></button>}
function QuoteCard({quote}:{quote:QuotePost}){const open=()=>window.dispatchEvent(new CustomEvent("nerdding:open-post",{detail:{postId:quote.id}}));return <article className="post-detail-quote" role="button" tabIndex={0} onClick={open} onKeyDown={e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();open()}}}><div className="post-detail-quote-label">QUOTED POST</div><UserButton user={quote.author} size="xs"/><p>{quote.text}</p>{quote.media?.length?<div className={`post-detail-media post-detail-media-${Math.min(quote.media.length,4)}`}>{quote.media.slice(0,4).map((m,i)=>m.publicUrl?(m.mimeType.startsWith("video/")?<video key={i} src={m.publicUrl} controls onClick={e=>e.stopPropagation()}/>:<img key={i} src={m.publicUrl} alt="" loading="lazy"/>):null)}</div>:null}<small>{ago(quote.createdAt)} · Open post</small></article>}

export default function PostDetailSurface({postId}:{postId:string}){
 const [post,setPost]=useState<Post|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const [text,setText]=useState("");const [busy,setBusy]=useState(false);const [saved,setSaved]=useState(false);
 const load=async()=>{setLoading(true);setError("");try{const r=await apiFetch<{data:Post}>(`/social/posts/${encodeURIComponent(postId)}`);setPost(r.data);setSaved(Boolean(r.data.saves&&r.data.saves>0))}catch(e){setError(e instanceof Error?e.message:"Post could not be loaded")}finally{setLoading(false)}};
 useEffect(()=>{void load()},[postId]);
 const addComment=async(parentId:string|null,body:string)=>{if(!getSavedUser()||busy)return;setBusy(true);try{await apiFetch(`/social/posts/${encodeURIComponent(postId)}/comments`,{method:"POST",body:JSON.stringify({body,parentId})});await load()}catch(e){setError(e instanceof Error?e.message:"Comment could not be posted")}finally{setBusy(false)}};
 if(loading)return <div className="nerdd-route-surface"><div className="skeleton-block skeleton-hero"/></div>;
 if(!post)return <div className="nerdd-route-surface"><div className="empty-state"><strong>Post could not be loaded.</strong><span>{error}</span><button className="outline-button" onClick={()=>void load()}>Try again</button></div></div>;
 const views=Number(post.views??0);
 return <div className="nerdd-route-surface"><div className="view post-detail-view"><button className="back-button" onClick={()=>nav("/home")}><ArrowLeft size={15}/> Back to feed</button><article className="post-detail-card"><UserButton user={post.author} size="md"/><span className="post-detail-time">{new Date(post.createdAt).toLocaleString()}</span><p className="post-detail-copy">{post.text}</p>{post.quotePost?<QuoteCard quote={post.quotePost}/>:null}{post.media?.length?<div className={`post-detail-media post-detail-media-${Math.min(post.media.length,4)}`}>{post.media.slice(0,4).map((m,i)=>m.publicUrl?(m.mimeType.startsWith("video/")?<video key={i} src={m.publicUrl} controls/>:<img key={i} src={m.publicUrl} alt="" loading="lazy"/>):null)}</div>:null}{post.project&&<button className="outline-button post-detail-project" onClick={()=>window.dispatchEvent(new CustomEvent("nerdding:open-project",{detail:{slug:post.project!.slug}}))}>Mentioned project: {post.project.name}</button>}{post.linkUrl?<a className="post-detail-link" href={post.linkUrl} target="_blank" rel="noreferrer"><Link2 size={14}/>{post.linkUrl.replace(/^https?:\/\//,"")}</a>:null}<div className="post-detail-actionbar"><div><span title="Likes"><Heart size={15}/> {post.likes}</span><span title="Comments"><MessageCircle size={15}/> {post.comments}</span><span title="Amplifies"><Activity size={15}/> {post.reposts}</span></div><div><span title="Views"><Eye size={15}/> {views}</span><button title="Save" className={saved?"action-active":""} onClick={async()=>{try{const r=await apiFetch<{data:{active:boolean}}>(`/posts/${post.id}/save`,{method:"POST"});setSaved(r.data.active);await load()}catch{}}}><Bookmark size={15} fill={saved?"currentColor":"none"}/> Save</button></div></div></article><section className="post-detail-comments"><div className="eyebrow">REPLIES</div><h2>Replies</h2><div className="post-detail-compose"><Avatar user={getSavedUser()} size="xs"/><input value={text} onChange={e=>setText(e.target.value)} placeholder="Write a reply…" onKeyDown={e=>{if(e.key==="Enter"&&text.trim()){void addComment(null,text.trim());setText("")}}}/><button disabled={!text.trim()||busy} onClick={()=>{void addComment(null,text.trim());setText("")}}><Send size={14}/></button></div><ThreadedCommentTree comments={post.commentsTree??[]} onReply={addComment}/></section></div></div>;
}
