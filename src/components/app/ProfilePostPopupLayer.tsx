"use client";

/**
 * Profile post details are rendered directly by ProfileSectionTabs inside
 * .profile-post-detail-slot. Keep this component as a no-op because the
 * application shell still imports it for backwards compatibility.
 */
export default function ProfilePostPopupLayer() {
  return null;
}
