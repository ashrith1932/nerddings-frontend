"use client";

import { useEffect, useState } from "react";
import { Activity, Bookmark, Loader2, Users, ArrowUpRight } from "lucide-react";
import { apiFetch } from "@/lib/api";

type Saved = { id: string; body: string; createdAt: string; author: { id: string; name: string; username: string; avatarUrl?: string | null } };
type Person = { id: string; name: string; username: string; accountType?: string; avatarUrl?: string | null };

const ago = (value: string) => { const s = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 1000)); if (s < 60) return "now"; if (s < 3600) return `${Math.floor(s / 60)}m`; if (s < 86400) return `${Math.floor(s / 3600)}h`; return `${Math.floor(s / 86400)}d`; };
const initials = (name: string) => name.split(/\s+/).filter(Boolean).map((x) => x[0]).join("").slice(0, 2).toUpperCase();

export default function YourNerddingsRoute() {
  const [saved, setSaved] = useState<Saved[]>([]); const [following, setFollowing] = useState<Person[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { let alive = true; setLoading(true); apiFetch<{ data: { savedPosts: Saved[]; following: Person[] } }>("/nerddings").then((r) => { if (!alive) return; setSaved(r.data.savedPosts ?? []); setFollowing(r.data.following ?? []); }).catch((e) => { if (alive) setError(e instanceof Error ? e.message : "Unable to load Your Nerddings"); }).finally(() => { if (alive) setLoading(false); }); return () => { alive = false; }; }, []);
  return <div className="your-nerddings-route"><style>{`.your-nerddings-route{min-height:calc(100dvh - 130px);padding:4px 0 50px;background:var(--paper,#f9f7f2)}.your-nerddings-route .yn-head{margin-bottom:18px}.your-nerddings-route h1{font:700 28px 'Space Grotesk',sans-serif;margin:3px 0 6px;color:#201c19}.your-nerddings-route p{font-size:11px;color:#8d837a}.your-nerddings-route .yn-grid{display:grid;grid-template-columns:minmax(0,1fr) 340px;gap:18px;align-items:start}.your-nerddings-route .yn-card{background:#fffdf9;border:1px solid #ded7cf;border-radius:14px;overflow:hidden}.your-nerddings-route .yn-card-head{display:flex;justify-content:space-between;align-items:center;padding:14px 16px;border-bottom:1px solid #eee8e1}.your-nerddings-route .yn-card-head strong{font:700 13px 'Space Grotesk',sans-serif}.your-nerddings-route .yn-item{display:flex;align-items:center;gap:11px;padding:13px 16px;border-bottom:1px solid #eee8e1}.your-nerddings-route .yn-item:last-child{border-bottom:0}.your-nerddings-route .yn-avatar{width:36px;height:36px;flex:0 0 36px;border-radius:50%;overflow:hidden;display:grid;place-items:center;background:#ebe5dd;font-size:10px;font-weight:800}.your-nerddings-route .yn-avatar img{width:100%;height:100%;object-fit:cover}.your-nerddings-route .yn-copy{min-width:0;flex:1}.your-nerddings-route .yn-copy strong{font-size:11px;color:#29241f}.your-nerddings-route .yn-copy small{display:block;margin-top:3px;font-size:9px;color:#938980}.your-nerddings-route .yn-post{padding:15px 16px;border-bottom:1px solid #eee8e1;cursor:pointer}.your-nerddings-route .yn-post:last-child{border-bottom:0}.your-nerddings-route .yn-post:hover{background:#faf7f1}.your-nerddings-route .yn-post p{font-size:11px;line-height:1.55;margin:8px 0 0;color:#403a34}.your-nerddings-route .yn-empty{padding:42px 20px;text-align:center;color:#948a81;font-size:10px}.your-nerddings-route .yn-error{padding:28px;text-align:center;color:#b64a30;font-size:11px}.your-nerddings-route .yn-skeleton{height:74px;border-bottom:1px solid #eee8e1;background:linear-gradient(90deg,#fffdf9 25%,#f1ece5 37%,#fffdf9 63%);background-size:400% 100%;animation:ynShimmer 1.2s infinite}@keyframes ynShimmer{0%{background-position:100% 0}100%{background-position:-100% 0}}@media(max-width:850px){.your-nerddings-route .yn-grid{grid-template-columns:1fr}.your-nerddings-route{min-height:0}}`}</style><div className="yn-head"><div className="eyebrow">YOUR ACTIVITY</div><h1>Your Nerddings</h1><p>Your saved posts and people you follow.</p></div>{error?<div className="yn-error">{error}</div>:<div className="yn-grid"><section className="yn-card"><div className="yn-card-head"><strong><Bookmark size={14}/> Saved posts</strong><span>{saved.length}</span></div>{loading?Array.from({length:5},(_,i)=><div className="yn-skeleton" key={i}/>):saved.length?saved.map((post)=><article className="yn-post" key={post.id} onClick={()=>{window.dispatchEvent(new CustomEvent("nerdding:open-profile-post",{detail:{postId:post.id}}));}}><strong>{post.author.name}</strong><small>@{post.author.username} · {ago(post.createdAt)}</small><p>{post.body}</p></article>):<div className="yn-empty"><Bookmark size={20}/><div>No saved posts yet.</div></div>}</section><aside className="yn-card"><div className="yn-card-head"><strong><Users size={14}/> Following</strong><span>{following.length}</span></div>{loading?Array.from({length:4},(_,i)=><div className="yn-skeleton" key={i}/>):following.length?following.map((person)=><button className="yn-item" key={person.id} onClick={()=>{window.history.pushState({},"",`/profile/${encodeURIComponent(person.username)}`);window.dispatchEvent(new PopStateEvent("popstate"));}}><span className="yn-avatar">{person.avatarUrl?<img src={person.avatarUrl} alt=""/>:initials(person.name)}</span><span className="yn-copy"><strong>{person.name}</strong><small>@{person.username}</small></span><ArrowUpRight size={14}/></button>):<div className="yn-empty"><Users size={20}/><div>You are not following anyone yet.</div></div>}</aside></div>}</div>;
}
