// @ts-nocheck
"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowUpRight, Bookmark, CalendarDays, Heart, MapPin, MessageCircle, Share2, Sparkles, UserPlus, Users, X } from "lucide-react";
import { apiFetch, getSavedUser } from "@/lib/api";
import PostDetailSurface from "@/components/app/PostDetailSurface";

const styles = `
.discovery-route{width:100%;color:#211d19}.discovery-grid{display:grid;grid-template-columns:minmax(0,1fr) 300px;gap:18px}.discovery-main{min-width:0}.discovery-rail{display:grid;gap:14px;align-content:start}.discovery-section{margin-bottom:24px}.discovery-head{display:flex;justify-content:space-between;align-items:end;gap:12px;margin-bottom:10px}.discovery-head h2{font-size:17px;margin:0}.discovery-head p{font-size:10px;color:#8d837a;margin:3px 0 0}.discovery-link{display:inline-flex;align-items:center;gap:3px;border:0;background:transparent;color:#cf5d34;font-size:10px;font-weight:800;cursor:pointer}.discovery-cats{display:flex;gap:8px;overflow:auto;scrollbar-width:none;padding-bottom:2px}.discovery-cats::-webkit-scrollbar,.discovery-projects::-webkit-scrollbar{display:none}.discovery-cat{white-space:nowrap;border:1px solid #ded7cf;background:#fffdf9;border-radius:11px;padding:10px 13px;font-size:10px;font-weight:800;cursor:pointer}.discovery-cat.active{background:#211d19;color:#fff;border-color:#211d19}.discovery-projects{display:flex;gap:12px;overflow:auto;scrollbar-width:none}.discovery-project-card{flex:0 0 178px;border:1px solid #e0d9d1;background:#fffdf9;border-radius:13px;padding:14px;text-align:left;cursor:pointer;transition:.18s}.discovery-project-card:hover{transform:translateY(-3px);box-shadow:0 14px 28px rgba(31,27,24,.08);border-color:#cfc5ba}.discovery-mark{width:38px;height:38px;border-radius:10px;background:#f0e8df;display:grid;place-items:center;font-weight:900;margin-bottom:11px}.discovery-project-card h3{font-size:12px;margin:0 0 5px}.discovery-project-card p{height:38px;overflow:hidden;font-size:9px;line-height:1.45;color:#756c64}.discovery-project-meta{display:flex;justify-content:space-between;margin-top:12px;color:#7e746c;font-size:9px}.discovery-chip{font-size:8px;padding:4px 7px;border-radius:99px;background:#f8efe8;color:#a74c2e}.discovery-posts{display:grid;gap:10px}.discovery-post{border:1px solid #e0d9d1;background:#fffdf9;border-radius:13px;padding:16px;cursor:pointer;transition:.18s}.discovery-post:hover{border-color:#cfc5ba;box-shadow:0 10px 24px rgba(31,27,24,.06);transform:translateY(-1px)}.discovery-author{display:flex;align-items:center;gap:9px}.discovery-avatar{width:34px;height:34px;border-radius:50%;overflow:hidden;background:#e8e2da;display:grid;place-items:center;font-size:10px;font-weight:900;flex:0 0 auto}.discovery-avatar img{width:100%;height:100%;object-fit:cover}.discovery-author-main{flex:1;min-width:0}.discovery-author-main strong{font-size:11px}.discovery-author-main small{font-size:9px;color:#938980;margin-left:4px}.discovery-time{font-size:8px;color:#9b928a}.discovery-body{font-size:11px;line-height:1.55;margin:11px 0}.discovery-media{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;margin-top:10px}.discovery-media img,.discovery-media video{width:100%;height:150px;object-fit:cover;border-radius:9px;background:#eee8e1}.discovery-post-tags{display:flex;flex-wrap:wrap;gap:6px}.discovery-post-tags span{font-size:8px;border-radius:99px;padding:4px 7px;background:#f8f1ea;color:#746b63}.discovery-actions{display:flex;justify-content:space-between;align-items:center;border-top:1px solid #eee8e1;margin-top:12px;padding-top:10px;color:#7b7169;font-size:9px}.discovery-actions-left,.discovery-actions-right{display:flex;align-items:center;gap:14px}.discovery-actions span{display:inline-flex;align-items:center;gap:4px}.discovery-rail-card{border:1px solid #ded7cf;background:#fffdf9;border-radius:13px;padding:15px}.discovery-rail-card h3{font-size:12px;margin:0}.discovery-tabs{display:flex;gap:14px;border-bottom:1px solid #eee8e1;margin:11px 0}.discovery-tabs button{border:0;background:none;padding:8px 0;font-size:9px;font-weight:800;color:#8e847c;cursor:pointer}.discovery-tabs button.active{color:#211d19;border-bottom:2px solid #211d19}.discovery-rank{display:flex;align-items:center;gap:8px;padding:10px 0;border-bottom:1px solid #eee8e1}.discovery-rank:last-child{border-bottom:0}.discovery-rank-no{width:15px;color:#9b9189;font-size:9px;font-weight:900}.discovery-rank-main{flex:1;min-width:0}.discovery-rank-main strong{font-size:10px;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.discovery-rank-main small{font-size:8px;color:#91877e}.discovery-score{font-size:9px;font-weight:800}.discovery-mini{display:block;width:100%;padding:10px 0;border:0;border-bottom:1px solid #eee8e1;background:none;text-align:left;cursor:pointer}.discovery-mini:last-child{border-bottom:0}.discovery-mini strong{display:block;font-size:10px}.discovery-mini small{display:block;font-size:8px;color:#91877e;margin-top:3px}.discovery-person{display:flex;align-items:center;gap:8px;padding:10px 0;border-bottom:1px solid #eee8e1}.discovery-person:last-child{border-bottom:0}.discovery-person-main{flex:1;min-width:0}.discovery-person-main strong{display:block;font-size:10px}.discovery-person-main small{font-size:8px;color:#91877e}.discovery-follow{border:1px solid #cfc5ba;background:#fff;border-radius:8px;padding:7px 8px;font-size:8px;font-weight:800;cursor:pointer}.discovery-empty{border:1px solid #e0d9d1;border-radius:13px;padding:32px;text-align:center;background:#fffdf9;color:#938980}.discovery-empty strong{display:block;margin-top:8px;color:#312c28;font-size:12px}.discovery-empty span{display:block;font-size:9px;margin-top:4px}.discovery-skeleton{height:110px;border-radius:12px;background:#ebe5de;position:relative;overflow:hidden}.discovery-skeleton:after{content:"";position:absolute;inset:0;transform:translateX(-100%);background:linear-gradient(90deg,transparent,rgba(255,255,255,.75),transparent);animation:disc-shimmer 1.1s infinite}@keyframes disc-shimmer{to{transform:translateX(100%)}}.discovery-event-hero{border:1px solid #e0d9d1;border-radius:15px;background:linear-gradient(135deg,#fbf5ee,#fffdf9);padding:25px;margin-bottom:16px}.discovery-event-hero h1{font-size:25px;margin:7px 0}.discovery-event-hero p{font-size:11px;color:#766d65;max-width:670px}.event-search{display:flex;gap:8px;max-width:760px}.event-search input{flex:1;height:39px;border:1px solid #ddd6cc;border-radius:9px;background:#fff;padding:0 11px;font:inherit;font-size:10px;outline:none}.event-btn{border:0;border-radius:9px;background:#211d19;color:#fff;padding:0 14px;font-weight:800;cursor:pointer}.event-btn.orange{background:#d65c31}.event-tabs{display:flex;gap:18px;border-bottom:1px solid #e7e0d9;margin-bottom:12px}.event-tabs button{border:0;background:none;padding:11px 0;font-size:10px;font-weight:800;color:#8e847b;cursor:pointer}.event-tabs button.active{color:#d65c31;border-bottom:2px solid #d65c31}.event-layout{display:grid;grid-template-columns:minmax(0,1fr) 280px;gap:14px}.event-list{border:1px solid #e0d9d1;border-radius:13px;background:#fffdf9;overflow:hidden}.event-row{display:grid;grid-template-columns:120px 68px minmax(0,1fr) auto;gap:12px;align-items:center;padding:14px;border-bottom:1px solid #eee8e1}.event-row:last-child{border-bottom:0}.event-cover{height:88px;border-radius:9px;background:linear-gradient(135deg,#29231e,#48382c);color:#fff;display:grid;place-items:center;font-weight:900;font-size:11px;text-align:center;padding:8px}.event-cover img{width:100%;height:100%;object-fit:cover;border-radius:9px}.event-date{border:1px solid #e1d9d2;border-radius:9px;padding:9px;text-align:center;background:#fffbf7}.event-date strong{display:block;color:#d65c31;font-size:16px}.event-date small{font-size:8px;color:#91877e}.event-main h3{font-size:12px;margin:0 0 4px}.event-main p{font-size:9px;line-height:1.45;color:#786f67;margin:0 0 6px}.event-meta{display:flex;gap:9px;flex-wrap:wrap;color:#8e847c;font-size:8px}.event-actions{display:flex;gap:7px;align-items:center}.event-actions button{border:1px solid #d8cfc5;background:#fff;border-radius:8px;padding:7px 9px;font-size:8px;font-weight:800;cursor:pointer}.event-actions button.primary{background:#d65c31;color:#fff;border-color:#d65c31}.event-filter,.event-calendar{border:1px solid #e0d9d1;border-radius:13px;background:#fffdf9;padding:14px}.event-filter{display:grid;gap:10px;align-content:start}.event-filter h3,.event-calendar h3{font-size:11px;margin:0}.event-filter label{display:grid;gap:5px;font-size:8px;color:#7e756d;font-weight:800}.event-filter select{height:36px;border:1px solid #ddd6cc;border-radius:9px;background:#fff;padding:0 9px;font:inherit;font-size:9px}.calendar-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:3px;margin-top:10px}.calendar-grid span{font-size:8px;color:#948b82;text-align:center;padding:4px 0}.calendar-grid button{border:0;background:none;padding:6px 0;border-radius:50%;font-size:8px;cursor:pointer}.calendar-grid button.has-event{background:#fff0e7;color:#d65c31;font-weight:900}.event-create{border:1px solid #e0d9d1;border-radius:13px;background:#fffdf9;padding:16px;margin-bottom:14px}.event-create-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.event-create-grid label{display:grid;gap:5px;font-size:8px;color:#7e756d;font-weight:800}.event-create-grid input,.event-create-grid textarea,.event-create-grid select{border:1px solid #ddd6cc;border-radius:9px;padding:9px;background:#fff;font:inherit;font-size:10px;outline:none}.event-create-grid textarea{min-height:90px;resize:vertical;grid-column:1/-1}.event-create-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:11px}.event-modal{position:fixed;inset:0;z-index:3100;background:rgba(25,20,16,.42);display:grid;place-items:center;padding:20px}.event-modal-card{width:min(780px,94vw);max-height:90vh;overflow:auto;border:1px solid #dcd4cb;background:#fffdf9;border-radius:16px;box-shadow:0 28px 90px rgba(0,0,0,.22);padding:20px}.event-modal-head{display:flex;justify-content:space-between;align-items:flex-start;gap:10px}.event-close{width:34px;height:34px;border:1px solid #ddd6cc;border-radius:50%;background:#fff;display:grid;place-items:center;cursor:pointer}.event-modal-card h2{font-size:22px;margin:5px 0}.event-modal-card p{font-size:11px;line-height:1.6;color:#766d65}.event-modal-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:15px}.event-chip{font-size:8px;padding:5px 8px;border-radius:99px;background:#f7eee7;color:#9d4d31}.event-success{font-size:9px;color:#2f7545;margin-top:8px}.event-error{font-size:9px;color:#a94027;margin-top:8px}
@media(max-width:1050px){.discovery-grid,.event-layout{grid-template-columns:1fr}.discovery-rail{grid-template-columns:repeat(3,minmax(0,1fr))}.event-row{grid-template-columns:105px 64px minmax(0,1fr)}}
@media(max-width:760px){.discovery-rail{grid-template-columns:1fr}.discovery-head{align-items:flex-start}.event-row{grid-template-columns:82px 52px minmax(0,1fr)}.event-actions{grid-column:3;flex-wrap:wrap}.event-create-grid{grid-template-columns:1fr}.event-create-grid textarea{grid-column:auto}.event-modal{padding:0;place-items:stretch}.event-modal-card{width:100%;height:100%;max-height:none;border-radius:0;border:0}.discovery-media img,.discovery-media video{height:120px}}
`;

type Project={id:string;name:string;slug:string;description:string;stage:string;score?:number};
type Post={id:string;text:string;createdAt:string;likes?:number;comments?:number;reposts?:number;saves?:number;author?:{id:string;name:string;username:string;avatarUrl?:string|null};hashtags?:string[];media?:Array<{publicUrl?:string|null;mimeType:string}>;project?:{id:string;name:string;slug:string;stage?:string}|null};
type Event={id:string;title:string;description:string;shortDescription?:string|null;eventType:string;format:string;startsAt:string;endsAt:string;location?:string;city?:string;timezone?:string;status:string;rsvpCount:number;maxAttendees?:number|null;creator?:{id:string;name:string;username:string;avatarUrl?:string|null};topics?:string[];coverImageUrl?:string|null;viewerGoing?:boolean;viewerSaved?:boolean;url?:string|null};

const initials=(name:string)=>name.split(/\s+/).filter(Boolean).map(v=>v[0]).join("").slice(0,2).toUpperCase();
const ago=(value:string)=>{const s=Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/1000));if(s<60)return"now";if(s<3600)return`${Math.floor(s/60)}m`;if(s<86400)return`${Math.floor(s/3600)}h`;return`${Math.floor(s/86400)}d`};
const nav=(path:string)=>{window.history.pushState({},"",path);window.dispatchEvent(new PopStateEvent("popstate"));};
function Avatar({user}:{user?:{name?:string;avatarUrl?:string|null}}){return <span className="discovery-avatar">{user?.avatarUrl?<img src={user.avatarUrl} alt=""/>:initials(user?.name??"N")}</span>}

export function ExploreRoute(){
  const [category,setCategory]=useState("All");const [payload,setPayload]=useState<any>(null);const [charts,setCharts]=useState<any>(null);const [events,setEvents]=useState<Event[]>([]);const [loading,setLoading]=useState(true);const [openPost,setOpenPost]=useState<Post|null>(null);const [chartTab,setChartTab]=useState("projects");const [following,setFollowing]=useState<Record<string,boolean>>({});
  useEffect(()=>{let alive=true;Promise.allSettled([apiFetch<any>("/social/explore/live"),apiFetch<any>("/charts"),apiFetch<any>("/events?limit=5")]).then(([a,b,c])=>{if(!alive)return;setPayload(a.status==="fulfilled"?a.value.data:null);setCharts(b.status==="fulfilled"?b.value.data:null);setEvents(c.status==="fulfilled"?(c.value.data??[]):[]);setLoading(false)});return()=>{alive=false}},[]);
  const categories=["All","AI / ML","Developer Tools","Web3","Health Tech","Education","Sustainability"];
  const posts:Post[]=useMemo(()=>{const rows=(payload?.stories??[]) as Post[];if(category==="All")return rows.slice(0,12);const needle=category.toLowerCase().replace(/\s*\/\s*/g,"");return rows.filter(p=>(p.hashtags??[]).some(t=>t.toLowerCase().includes(needle))||p.project?.name?.toLowerCase().includes(category.toLowerCase())).slice(0,12)},[payload,category]);
  const projects:Project[]=useMemo(()=>{const rows=(charts?.topProjects??[]) as Project[];return category==="All"?rows.slice(0,10):rows.filter(p=>p.name.toLowerCase().includes(category.toLowerCase())).slice(0,10)},[charts,category]);
  const builderRows=(charts?.risingBuilders??[]).slice(0,5);const chartRows=chartTab==="projects"?(charts?.topProjects??[]):chartTab==="builders"?(charts?.risingBuilders??[]):(charts?.trendingStartups??[]);
  const follow=async(id:string)=>{try{const r=await apiFetch<any>(`/social/users/${encodeURIComponent(id)}/follow`,{method:"POST"});setFollowing(v=>({...v,[id]:Boolean(r.data?.active)}))}catch{}};
  return <div className="discovery-route"><style>{styles}</style><div className="discovery-grid"><main className="discovery-main">
    <section className="discovery-section"><div className="discovery-head"><div><h2>Explore by category</h2><p>Discover the people, projects and ideas shaping Nerdding.</p></div></div><div className="discovery-cats">{categories.map(c=><button key={c} className={`discovery-cat ${category===c?"active":""}`} onClick={()=>setCategory(c)}>{c}</button>)}</div></section>
    <section className="discovery-section"><div className="discovery-head"><div><h2>Trending projects</h2><p>Top projects gaining momentum in the community</p></div><button className="discovery-link" onClick={()=>nav("/charts")}>View all <ArrowUpRight size={12}/></button></div>{loading?<div className="discovery-projects">{[1,2,3,4].map(i=><div className="discovery-project-card" key={i}><div className="discovery-skeleton"/></div>)}</div>:projects.length?<div className="discovery-projects">{projects.map(project=><button className="discovery-project-card" key={project.id} onClick={()=>window.dispatchEvent(new CustomEvent("nerdding:project-open",{detail:{slug:project.slug}}))}><span className="discovery-mark">{project.name.slice(0,1).toUpperCase()}</span><h3>{project.name}</h3><p>{project.description}</p><div className="discovery-post-tags"><span className="discovery-chip">{project.stage}</span></div><div className="discovery-project-meta"><span>↗ {Math.round(Number(project.score??0))}</span><span>Live</span></div></button>)}</div>:<div className="discovery-empty"><Sparkles size={18}/><strong>No projects trending yet</strong><span>As builders publish and collaborate, momentum will appear here.</span></div>}</section>
    <section className="discovery-section"><div className="discovery-head"><div><h2>Popular posts</h2><p>Top posts from across Nerdding</p></div></div>{loading?<div style={{display:"grid",gap:10}}>{[1,2,3].map(i=><div className="discovery-skeleton" key={i}/>)}</div>:posts.length?<div className="discovery-posts">{posts.map(post=><article className="discovery-post" key={post.id} onClick={()=>setOpenPost(post)}><div className="discovery-author"><Avatar user={post.author}/><div className="discovery-author-main"><strong>{post.author?.name??"Nerdding member"}</strong><small>@{post.author?.username??"member"}</small></div><span className="discovery-time">{ago(post.createdAt)}</span></div><div className="discovery-body">{post.text}</div>{post.media?.length?<div className="discovery-media">{post.media.slice(0,2).map((m,i)=>m.mimeType.startsWith("video/")?<video key={i} src={m.publicUrl??undefined} controls/>:<img key={i} src={m.publicUrl??undefined} alt="" loading="lazy"/>)}</div>:null}<div className="discovery-post-tags">{post.project&&<span>{post.project.name}</span>}{(post.hashtags??[]).slice(0,3).map(tag=><span key={tag}>#{tag.replace(/^#/i,"")}</span>)}</div><div className="discovery-actions"><div className="discovery-actions-left"><span><Heart size={13}/> {post.likes??0}</span><span><MessageCircle size={13}/> {post.comments??0}</span><span><Share2 size={13}/> {post.reposts??0}</span></div><div className="discovery-actions-right"><span>Views</span><span><Bookmark size={13}/> {post.saves??0}</span></div></div></article>)}</div>:<div className="discovery-empty"><Sparkles size={18}/><strong>No posts in this category yet.</strong><span>Explore another category to keep discovering.</span></div>}</section>
  </main><aside className="discovery-rail">
    <section className="discovery-rail-card"><div className="discovery-head"><div><h3>Top charts</h3></div><button className="discovery-link" onClick={()=>nav("/charts")}>View full <ArrowUpRight size={11}/></button></div><div className="discovery-tabs">{[["projects","Projects"],["builders","Builders"],["posts","Posts"]].map(([value,label])=><button key={value} className={chartTab===value?"active":""} onClick={()=>setChartTab(value)}>{label}</button>)}</div>{chartRows.slice(0,5).map((row:any,i:number)=><div className="discovery-rank" key={row.id??i}><span className="discovery-rank-no">{i+1}</span><Avatar user={{name:row.name??row.startupName??"Item",avatarUrl:row.avatarUrl}}/><span className="discovery-rank-main"><strong>{row.name??row.startupName}</strong><small>{row.username?`@${row.username}`:row.stage??row.industry??"Live ranking"}</small></span><span className="discovery-score">{row.score!=null?Number(row.score).toFixed(0):"—"}</span></div>)}</section>
    <section className="discovery-rail-card"><div className="discovery-head"><div><h3>Events you might like</h3></div><button className="discovery-link" onClick={()=>nav("/events")}>View all <ArrowUpRight size={11}/></button></div>{events.slice(0,4).map(event=><button className="discovery-mini" key={event.id} onClick={()=>{nav("/events");window.setTimeout(()=>window.dispatchEvent(new CustomEvent("nerdding:event-open",{detail:{id:event.id}})),0)}}><strong>{event.title}</strong><small>{new Date(event.startsAt).toLocaleDateString()} · {event.location||event.format} · {event.rsvpCount} going</small></button>)}</section>
    <section className="discovery-rail-card"><div className="discovery-head"><div><h3>People to follow</h3></div></div>{builderRows.map((builder:any)=><div className="discovery-person" key={builder.id}><Avatar user={builder}/><span className="discovery-person-main"><strong>{builder.name}</strong><small>@{builder.username}</small></span><button className="discovery-follow" onClick={()=>void follow(builder.id)}>{following[builder.id]?"Following":"Follow"}</button></div>)}</section>
  </aside></div>{openPost&&<div className="event-modal" role="dialog" aria-modal="true"><div className="event-modal-card"><div className="event-modal-head"><div><div className="eyebrow">POST</div><h2>{openPost.author?.name??"Nerdding member"}</h2></div><button className="event-close" onClick={()=>setOpenPost(null)}><X size={17}/></button></div><PostDetailSurface postId={openPost.id}/></div></div>}</div>;
}

export function EventsRoute(){
  const [events,setEvents]=useState<Event[]>([]);const [loading,setLoading]=useState(true);const [tab,setTab]=useState("upcoming");const [query,setQuery]=useState("");const [type,setType]=useState("all");const [format,setFormat]=useState("all");const [location,setLocation]=useState("");const [selected,setSelected]=useState<Event|null>(null);const [creating,setCreating]=useState(false);const [saving,setSaving]=useState(false);const [saved,setSaved]=useState<Record<string,boolean>>({});const [going,setGoing]=useState<Record<string,boolean>>({});const [error,setError]=useState("");const user=getSavedUser();const isAgent=user?.accountType==="agent";const createFormRef = useRef<HTMLFormElement>(null);
  const load=async()=>{setLoading(true);setError("");try{const params=new URLSearchParams();if(query.trim())params.set("q",query.trim());if(type!=="all")params.set("eventType",type);if(format!=="all")params.set("format",format);if(location.trim())params.set("location",location.trim());if(tab==="past")params.set("past","true");const response=await apiFetch<any>(`/events?${params.toString()}`);setEvents(response.data??[])}catch(e){setError(e instanceof Error?e.message:"Unable to load events") }finally{setLoading(false)}};
  useEffect(()=>{void load()},[tab,type,format]);
  useEffect(()=>{const open=async(event:Event)=>{const detail=(event as CustomEvent<{id?:string}>).detail;if(!detail?.id)return;try{const r=await apiFetch<any>(`/events/${encodeURIComponent(detail.id)}`);setSelected(r.data);setSaved(v=>({...v,[detail.id!]:Boolean(r.data?.viewerSaved)}));setGoing(v=>({...v,[detail.id!]:Boolean(r.data?.viewerGoing)}))}catch{}};window.addEventListener("nerdding:event-open",open as EventListener);const id=new URLSearchParams(window.location.search).get("event");if(id)void (async()=>{try{const r=await apiFetch<any>(`/events/${encodeURIComponent(id)}`);setSelected(r.data);setSaved(v=>({...v,[id]:Boolean(r.data?.viewerSaved)}));setGoing(v=>({...v,[id]:Boolean(r.data?.viewerGoing)}))}catch{}})();return()=>window.removeEventListener("nerdding:event-open",open as EventListener)},[]);
  const mutateEvent=async(id:string,action:"rsvp"|"save",value:boolean)=>{try{if(action==="rsvp"){const r=value?await apiFetch<any>(`/events/${encodeURIComponent(id)}/rsvp`,{method:"POST",body:JSON.stringify({status:"going"})}):await apiFetch<any>(`/events/${encodeURIComponent(id)}/rsvp`,{method:"DELETE"});setGoing(v=>({...v,[id]:value}));if(selected?.id===id)setSelected(r.data)}else{const r=value?await apiFetch<any>(`/events/${encodeURIComponent(id)}/bookmark`,{method:"POST"}):await apiFetch<any>(`/events/${encodeURIComponent(id)}/bookmark`,{method:"DELETE"});setSaved(v=>({...v,[id]:Boolean(r.data?.active)}))}}catch(e){setError(e instanceof Error?e.message:"Unable to update event")}};
  const create=async(form:FormData,status:"draft"|"published")=>{setSaving(true);setError("");try{if(!form.get("title")||!form.get("description")){setError("Please fill in title and description.");setSaving(false);return;}if(!form.get("startsAt")||!form.get("endsAt")){setError("Please fill in start and end date/time.");setSaving(false);return;}const payload={title:String(form.get("title")),shortDescription:String(form.get("shortDescription")||""),description:String(form.get("description")),eventType:String(form.get("eventType")),format:String(form.get("format")),location:String(form.get("location")||""),city:String(form.get("city")||""),country:String(form.get("country")||""),onlineUrl:String(form.get("onlineUrl")||"")||null,coverImageUrl:String(form.get("coverImageUrl")||"")||null,startsAt:new Date(String(form.get("startsAt"))).toISOString(),endsAt:new Date(String(form.get("endsAt"))).toISOString(),timezone:Intl.DateTimeFormat().resolvedOptions().timeZone,maxAttendees:form.get("maxAttendees")?Number(form.get("maxAttendees")):null,topics:String(form.get("topics")||"").split(",").map(v=>v.trim()).filter(Boolean),status};await apiFetch("/events",{method:"POST",body:JSON.stringify(payload)});setCreating(false);await load()}catch(e){setError(e instanceof Error?e.message:"Unable to create event")}finally{setSaving(false)}};
  const now=Date.now();const weekend=events.filter(e=>{const day=new Date(e.startsAt).getDay();return day===0||day===6});const month=events.filter(e=>new Date(e.startsAt).getMonth()===new Date().getMonth()&&new Date(e.startsAt).getFullYear()===new Date().getFullYear());const visible=tab==="weekend"?weekend:tab==="month"?month:events.filter(e=>tab==="past"?new Date(e.endsAt).getTime()<now:new Date(e.endsAt).getTime()>=now);
  const monthDays=useMemo(()=>{const d=new Date();const first=new Date(d.getFullYear(),d.getMonth(),1).getDay();const count=new Date(d.getFullYear(),d.getMonth()+1,0).getDate();return {first,count,month:d.getMonth(),year:d.getFullYear()}},[]);const eventDays=new Set(events.map(e=>{const d=new Date(e.startsAt);return d.getMonth()===monthDays.month&&d.getFullYear()===monthDays.year?d.getDate():-1}));
  return <div className="discovery-route"><style>{styles}</style><section className="discovery-event-hero"><div className="eyebrow">MEET · LEARN · BUILD</div><h1>Discover events for builders</h1><p>Meet, learn, and build with the Nerdding community.</p><div className="event-search"><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")void load()}} placeholder="Search events by name, topic, or location"/><button className="event-btn" onClick={()=>void load()}>Search</button>{isAgent&&<button className="event-btn orange" onClick={()=>setCreating(v=>!v)}>+ Create event</button>}</div></section>
    {creating&&isAgent&&<section className="event-create"><div className="discovery-head"><div><h2>Create event</h2><p>Official community events can only be created by verified Agents.</p></div></div><form ref={createFormRef} onSubmit={e=>e.preventDefault()}><div className="event-create-grid"><label>Title<input name="title" required maxLength={180}/></label><label>Type<select name="eventType" defaultValue="Meetup"><option>Meetup</option><option>Workshop</option><option>Conference</option><option>Hackathon</option><option>Networking</option><option>Webinar</option><option>Other</option></select></label><label>Format<select name="format" defaultValue="in_person"><option value="in_person">In person</option><option value="online">Online</option><option value="hybrid">Hybrid</option></select></label><label>Location<input name="location" placeholder="Venue / location"/></label><label>City<input name="city"/></label><label>Country<input name="country"/></label><label>Start<input name="startsAt" type="datetime-local" required/></label><label>End<input name="endsAt" type="datetime-local" required/></label><label>Timezone<input name="timezone" defaultValue={Intl.DateTimeFormat().resolvedOptions().timeZone}/></label><label>Max attendees<input name="maxAttendees" type="number" min="1"/></label><label>Topics<input name="topics" placeholder="AI / ML, Startups"/></label><label>Online URL<input name="onlineUrl" type="url"/></label><label>Cover image URL<input name="coverImageUrl" type="url"/></label><label>Short description<input name="shortDescription" maxLength={500}/></label><label style={{gridColumn:"1/-1"}}>Description<textarea name="description" required maxLength={5000}/></label></div><div className="event-create-actions"><button type="button" className="discovery-follow" onClick={()=>setCreating(false)}>Cancel</button><button type="button" className="discovery-follow" disabled={saving} onClick={()=>void create(new FormData(createFormRef.current!),"draft")}>Save draft</button><button type="button" className="event-btn orange" disabled={saving} onClick={()=>void create(new FormData(createFormRef.current!),"published")}>{saving?"Publishing…":"Publish event"}</button></div></form></section>}
    <div className="event-tabs"><button className={tab==="upcoming"?"active":""} onClick={()=>setTab("upcoming")}>Upcoming</button><button className={tab==="weekend"?"active":""} onClick={()=>setTab("weekend")}>This weekend</button><button className={tab==="month"?"active":""} onClick={()=>setTab("month")}>This month</button><button className={tab==="past"?"active":""} onClick={()=>setTab("past")}>Past events</button></div>
    <div className="event-layout"><main className="event-list">{loading?[1,2,3,4].map(i=><div className="discovery-skeleton" key={i}/>):visible.length?visible.map(event=>{const d=new Date(event.startsAt);const isGoing=Boolean(going[event.id]??event.viewerGoing);const isSaved=Boolean(saved[event.id]??event.viewerSaved);return <article className="event-row" key={event.id}><div className="event-cover">{event.coverImageUrl?<img src={event.coverImageUrl} alt=""/>:event.title}</div><div className="event-date"><strong>{d.toLocaleDateString(undefined,{day:"2-digit"})}</strong><small>{d.toLocaleDateString(undefined,{month:"short"}).toUpperCase()}</small></div><div className="event-main"><h3>{event.title}</h3><p>{event.shortDescription||event.description}</p><div className="event-meta"><span><MapPin size={10}/> {event.location||event.format}</span><span><Users size={10}/> {event.rsvpCount} going</span><span>{event.eventType}</span></div>{event.creator&&<div className="event-meta">Hosted by @{event.creator.username}</div>}</div><div className="event-actions"><button onClick={()=>setSelected(event)}>View details</button><button className={isGoing?"primary":""} onClick={()=>void mutateEvent(event.id,"rsvp",!isGoing)}>{isGoing?"✓ Going":"I’m going"}</button><button onClick={()=>void mutateEvent(event.id,"save",!isSaved)} aria-label="Save event"><Bookmark size={13} fill={isSaved?"currentColor":"none"}/></button></div></article>}):<div className="discovery-empty"><CalendarDays size={18}/><strong>No events found</strong><span>Try another filter or search term.</span></div>}</main><aside className="discovery-rail"><div className="event-filter"><h3>Find events</h3><label>Event type<select value={type} onChange={e=>setType(e.target.value)}><option value="all">All types</option><option>Meetup</option><option>Workshop</option><option>Conference</option><option>Hackathon</option><option>Networking</option><option>Webinar</option></select></label><label>Format<select value={format} onChange={e=>setFormat(e.target.value)}><option value="all">All formats</option><option value="in_person">In person</option><option value="online">Online</option><option value="hybrid">Hybrid</option></select></label><label>Location<input value={location} onChange={e=>setLocation(e.target.value)} placeholder="City / country"/></label><button className="event-btn orange" onClick={()=>void load()}>Apply filters</button><button className="discovery-follow" onClick={()=>{setType("all");setFormat("all");setLocation("");setQuery("")}}>Clear filters</button></div><div className="event-calendar"><div className="discovery-head"><div><h3>{new Date().toLocaleDateString(undefined,{month:"long",year:"numeric"})}</h3></div><CalendarDays size={14}/></div><div className="calendar-grid">{["S","M","T","W","T","F","S"].map(day=><span key={day}>{day}</span>)}{Array.from({length:monthDays.first},(_,i)=><span key={`blank-${i}`}/>) }{Array.from({length:monthDays.count},(_,i)=>{const day=i+1;return <button key={day} className={eventDays.has(day)?"has-event":""} onClick={()=>{const match=events.find(e=>new Date(e.startsAt).getDate()===day);if(match)setSelected(match)}}>{day}</button>})}</div></div><div className="discovery-rail-card"><div className="discovery-head"><div><h3>Events you might like</h3></div><button className="discovery-link" onClick={()=>nav("/events")}>View all <ArrowUpRight size={11}/></button></div>{events.slice(0,3).map(event=><button key={event.id} className="discovery-mini" onClick={()=>setSelected(event)}><strong>{event.title}</strong><small>{new Date(event.startsAt).toLocaleDateString()} · {event.rsvpCount} going</small></button>)}</div></aside></div>
    {error&&<div className="event-error">{error}</div>}
    {selected&&<div className="event-modal" role="dialog" aria-modal="true"><div className="event-modal-card"><div className="event-modal-head"><div><div className="eyebrow">{selected.eventType} · {selected.status}</div><h2>{selected.title}</h2><div className="discovery-post-tags">{(selected.topics??[]).map(topic=><span className="event-chip" key={topic}>{topic}</span>)}</div></div><button className="event-close" onClick={()=>setSelected(null)}><X size={17}/></button></div>{selected.coverImageUrl&&<img src={selected.coverImageUrl} alt="" style={{width:"100%",maxHeight:300,objectFit:"cover",borderRadius:12,margin:"14px 0"}}/>}<p>{selected.description}</p><div className="event-meta"><span><CalendarDays size={11}/> {new Date(selected.startsAt).toLocaleString()} – {new Date(selected.endsAt).toLocaleString()}</span><span><MapPin size={11}/> {selected.location||selected.format}</span><span><Users size={11}/> {selected.rsvpCount}{selected.maxAttendees?` / ${selected.maxAttendees}`:""} going</span></div>{selected.creator&&<button className="discovery-person" onClick={()=>nav(`/profile/${encodeURIComponent(selected.creator!.username)}`)}><Avatar user={selected.creator}/><span className="discovery-person-main"><strong>{selected.creator.name}</strong><small>@{selected.creator.username} · Official Agent</small></span><UserPlus size={14}/></button>}<div className="event-modal-actions"><button className="event-btn orange" onClick={()=>void mutateEvent(selected.id,"rsvp",!Boolean(going[selected.id]??selected.viewerGoing))}>{Boolean(going[selected.id]??selected.viewerGoing)?"✓ Going":"I’m going"}</button><button className="discovery-follow" onClick={()=>void mutateEvent(selected.id,"save",!Boolean(saved[selected.id]??selected.viewerSaved))}><Bookmark size={13}/> {Boolean(saved[selected.id]??selected.viewerSaved)?"Saved":"Save"}</button><button className="discovery-follow"><Share2 size={13}/> Share</button>{selected.url&&<a className="discovery-follow" href={selected.url} target="_blank" rel="noreferrer">Open link <ArrowUpRight size={12}/></a>}</div></div></div>}
  </div>;
}
